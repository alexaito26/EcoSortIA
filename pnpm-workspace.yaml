packages:
  - "apps/*"
  - "packages/*"
  - "scripts/*"

# pnpm 11 en Vercel: hay que autorizar explicitamente los postinstall.
# Sin esto el install falla con ERR_PNPM_IGNORED_BUILDS.
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
